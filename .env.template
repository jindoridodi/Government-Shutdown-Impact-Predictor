# .env.template
# Copy this file to `.env` and replace placeholder values with real credentials.
# DO NOT commit the `.env` file with real secrets to version control.

# IBM watsonx.ai credentials
API_KEY=your_ibm_api_key_here
PROJECT_ID=your_project_id_here

# Endpoint configuration (default used in utils/config.py)
ENDPOINT=https://us-south.ml.cloud.ibm.com

# Quick usage:
# 1) Copy this file: `copy .env.template .env` (Windows cmd)
# 2) Edit `.env` and fill your API_KEY and PROJECT_ID
# 3) Install deps: `pip install -r requirements.txt`
# 4) Run predictor: `python models/predictor.py`
# 5) Run the desktop UI: `python main.py` (requires PyQt6 + WebEngine)
